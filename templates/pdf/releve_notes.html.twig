<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Relevé de notes</title>
    <style>
        body { font-family: Arial, sans-serif; }
        table { width: 100%; border-collapse: collapse; }
        th, td { border: 1px solid black; padding: 5px; }
        th { background-color: #f2f2f2; }
    </style>
</head>
<body>
    <h1>Relevé de notes</h1>
    <p>Nom : {{ student.Name }}</p>
    <p>Matricule : {{ student.studentID }}</p>
    <p>Niveau : {{ student.level }}</p>

    <table>
        <thead>
            <tr>
                <th>UE</th>
                <th>Moyenne</th>
                <th>Grade</th>
                <th>Crédits</th>
            </tr>
        </thead>
        <tbody>
            {% for grade in grades %}
            <tr>
                <td>{{ grade.ue }}</td>
                <td>{{ grade.average|number_format(2) }}</td>
                <td>{{ grade.grade }}</td>
                <td>{{ grade.credits }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

    <p>MPC : {{ mpc|number_format(2) }}</p>
</body>
</html>