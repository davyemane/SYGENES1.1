<!DOCTYPE html>
<html lang="{{ app.request.locale }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Choose Language - SYGENES</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
        }
        .text-shadow {
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        }
        .btn {
            background-color: #ea4335;
            color: white;
            transition: background-color 0.3s, transform 0.3s;
        }
        .btn:hover, .btn.active {
            background-color: #1a73e8;
            transform: scale(1.05);
        }
        .btn.active {
            box-shadow: 0 0 0 3px #1a73e8, 0 0 0 6px rgba(26, 115, 232, 0.3);
        }
    </style>
</head>
<body class="text-gray-900 min-h-screen flex flex-col">
    <main class="flex-grow flex items-center justify-center px-4">
        <div class="text-center language-switcher">
            <h1 class="text-4xl font-bold mb-10 text-shadow">{{ 'language-message'|trans }}</h1>
            <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6 max-w-3xl mx-auto">
                <a href="{{ path('change_language', {'locale': 'en'}) }}" class="btn font-semibold py-4 px-6 rounded-lg {{ app.request.locale == 'en' ? 'active' : '' }}">{{ 'english-language'|trans }}</a>
                <a href="{{ path('change_language', {'locale': 'fr'}) }}" class="btn font-semibold py-4 px-6 rounded-lg {{ app.request.locale == 'fr' ? 'active' : '' }}">{{ 'french-language'|trans }}</a>
                <a href="{{ path('change_language', {'locale': 'es'}) }}" class="btn font-semibold py-4 px-6 rounded-lg {{ app.request.locale == 'es' ? 'active' : '' }}">{{ 'spanish-language'|trans }}</a>
            </div>
            <div class="mt-16">
                <a href="{{path('school_new')}}" class="btn font-semibold py-4 px-8 rounded-lg">{{ 'next-language-btn'|trans }}</a>
            </div>
        </div>
    </main>
    <footer class="p-6 text-center">
        <p class="text-sm opacity-75">&copy; 2024 ESIGN. All rights reserved.</p>
    </footer>
</body>
</html>